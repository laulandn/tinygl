These require SDL.

These will not build if you use the included AGL without SDL support.

NOTE: Be careful with SDL usage.

If you use an SDL already linked with TinyGL's agl, you may be init'd SDL TWICE.