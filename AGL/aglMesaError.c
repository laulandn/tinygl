/* THIS WILL OVERRIDE THE MESA'S INTERNAL ERROR HANDLER TO A NEW */#include <string.h>#include <OSUtils.h>#include "types.h"void gl_problem( GLcontext *ctx, const char *s ){	char	str[255];	int 	len;		len = (strlen(s) < 254) ? strlen(s) : 254;		memcpy(&str[1],s,len);	str[0] = len;		DebugStr((StringPtr)str);}void gl_warning( GLcontext *ctx, const char *s ){#ifdef DEBUG	char	str[255];	int 	len;		len = (strlen(s) < 254) ? strlen(s) : 254;		memcpy(&str[1],s,len);	str[0] = len;		DebugStr((StringPtr)str);#endif}